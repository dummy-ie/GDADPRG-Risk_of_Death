#ifndef ENUM_SCENE_TAG_HPP
#define ENUM_SCENE_TAG_HPP

enum class SceneTag : int {
    TITLE_SCREEN = 0,
    GAME_SPACE,
    // Additional scenes that split the game space?
    LEADERBOARDS
};

#endif
#ifndef ENUM_SFX_TYPE_HPP
#define ENUM_SFX_TYPE_HPP

enum class SFXType : int {
    PLAYER_SHOOT = 0,
    RELOAD,
    HURT,

    HITMARKER,

    HEALTH,
    DAMAGE,
    PIERCING,
    INVINCIBILITY,
    FREEZE,

    SHINY
};

#endif

